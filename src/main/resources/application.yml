spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
    #更新或创建数据表结构
      ddl-auto: update
    show-sql: true
    #控制台显示sql